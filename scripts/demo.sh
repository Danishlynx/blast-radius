#!/usr/bin/env bash
# Placeholder: full golden path (break -> detect -> act -> gate) lands on Day 4-5 of the build plan.
. "$(dirname "$0")/lib.sh"
die "not implemented yet — full golden path (break -> detect -> act -> gate) arrives on Day 4-5"
